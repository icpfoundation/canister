

type imageStoreRes = variant{
    Ok:null;
    Err:text;
};
service:(principal) -> {
    image_store:(principal,nat64, blob) ->(imageStoreRes);
    get_image:(principal,nat64) -> (blob) query;
    update_manage_canister:(principal) -> ();
}